import React from "react";

const CheckoutForm = () => {
  return (
    <div className="checkoutFallback">
      The payment flow now uses Razorpay on the Pay page.
    </div>
  );
};

export default CheckoutForm;
